version: 1
frontend:
  phases:
    preBuild:
      commands:
        - python --version
        - pip install --upgrade pip
    build:
      commands:
        - pip install -r requirements.txt
  artifacts:
    baseDirectory: /
    files:
      - '**/*'